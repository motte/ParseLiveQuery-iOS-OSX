import XCTest
@testable import ParseLiveQuery_iOS_OSX

final class ParseLiveQuery_iOS_OSXTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ParseLiveQuery_iOS_OSX().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
